{
  "name": "opure-nexus",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd activity/client && npm install && npm run build",
    "dev": "cd activity/client && npm run dev",
    "start": "cd activity/client && npm run preview"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "vercel": "^44.7.2",
    "vite": "^7.0.6"
  }
}
